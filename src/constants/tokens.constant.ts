/**
 * @fileoverview Dependency injection tokens for the coordinator package.
 *
 * DI tokens from `@stackra/contracts` should be imported directly:
 * `import { COORDINATOR_CONFIG, TAB_COORDINATOR, TAB_LOCK_MANAGER } from "@stackra/contracts";`
 *
 * These re-exports exist for convenience within the package.
 *
 * @module @stackra/ts-coordinator
 * @category Constants
 */

export { COORDINATOR_CONFIG, TAB_COORDINATOR, TAB_LOCK_MANAGER } from "@stackra/contracts";
